# configuration file
# pause between restarts(secs)
PAUSE_TIME=10
# session name to use
TMUX_SESSION=always_run
# if using key for authentification(cpk.sh, killallk.sh, statusk.sh)
# you can write path to it here:
# note if key as parameter used when calling any of such file
# (cpk.sh, killallk.sh, statusk.sh) key parameter is used instead of 
# DEFAULT_KEY_PATH. If DEFAULT_KEY_PATH is empty, KEY_PATH(as parameter used).
# example DEFAULT_KEY_PATH=/aws/key_pair_AWS.pem
DEFAULT_KEY_PATH=
